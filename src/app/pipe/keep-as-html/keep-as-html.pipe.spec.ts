import { KeepAsHtmlPipe } from './keep-as-html.pipe';

describe('KeepAsHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new KeepAsHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
