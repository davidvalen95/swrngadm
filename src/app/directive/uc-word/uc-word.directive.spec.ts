import { UcWordDirective } from './uc-word.directive';

describe('UcWordDirective', () => {
  it('should create an instance', () => {
    const directive = new UcWordDirective();
    expect(directive).toBeTruthy();
  });
});
