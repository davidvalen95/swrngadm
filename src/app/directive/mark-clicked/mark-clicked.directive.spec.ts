import { MarkClickedDirective } from './mark-clicked.directive';

describe('MarkClickedDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkClickedDirective();
    expect(directive).toBeTruthy();
  });
});
