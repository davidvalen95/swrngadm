import { StatusParsingDirective } from './status-parsing.directive';

describe('StatusParsingDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusParsingDirective();
    expect(directive).toBeTruthy();
  });
});
